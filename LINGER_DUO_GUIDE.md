# Руководство по режиму Linger Duo

## 🎯 Обзор

Режим **Linger Duo** позволяет создавать встречи строго на двоих — эксклюзивные события, где создатель выбирает одного участника из списка заявок.

## ✨ Особенности

### 1. Активация режима (Gesture & Flip)

**Активация:**
1. Нарисуйте непрерывный круг пальцем на экране
2. Нажмите в центр нарисованного круга
3. Экран развернется на 180 градусов (3D анимация)
4. Фон изменится на темный эксклюзивный градиент (глубокий фиолетовый/темно-синий)

**Визуальные эффекты:**
- Анимация разворота экрана: `transform: rotateY(180deg)` (1.2 секунды)
- Темный градиент: `bg-gradient-to-br from-purple-950 via-indigo-950 to-black`
- Индикатор режима Duo в верхней части экрана

**Выход из режима:**
- Нажмите кнопку "Выход" в индикаторе Duo
- Обратная анимация разворота (1.2 секунды)
- Возврат к стандартному интерфейсу

### 2. Логика Duo-событий (Тет-а-тет)

**Создание события в режиме Duo:**
- Любое событие, созданное в режиме Duo, автоматически становится эксклюзивным (`is_duo_event = true`)
- Событие предназначено только для двоих: создателя и выбранного участника

**Запросы на участие:**
- Любой пользователь (мужчина или женщина) может отправить запрос на участие
- Запросы видны только создателю события
- Остальные пользователи видят только само событие на карте

**Выбор участника:**
- Создатель события видит список всех заявок
- Можно фильтровать заявки по полу (Все / Мужчины / Женщины)
- После выбора одного участника остальные заявки автоматически отклоняются
- Прием новых заявок закрывается после выбора участника

**Приватность:**
- Список людей, отправивших запрос, виден только создателю
- Остальные пользователи видят только само событие на карте
- После выбора участника другие пользователи видят сообщение "Участник уже выбран"

### 3. Групповой режим (Стандартный)

**Обычный режим (не перевернутый):**
- Все созданные события остаются групповыми
- Любой желающий может вступать в событие без лимита
- Нет ограничения на количество участников (или лимит устанавливается создателем)

## 👤 Поля профиля

### Поле "Пол" (Gender)

**Добавлено в профиль:**
- Мужской / Male
- Женский / Female
- Не указывать / Prefer not to say

**Отображение:**
- Поле отображается рядом с именем пользователя в списке заявок
- Позволяет создателю события фильтровать заявки по полу
- Используется для создания более целенаправленных встреч

**Настройка:**
- Перейдите в Профиль → Настройки → Редактирование профиля
- Выберите поле "Пол" из выпадающего списка
- Сохраните изменения

## 🗄️ Структура БД

### Таблица `impulses` (События)

```sql
ALTER TABLE impulses 
ADD COLUMN is_duo_event BOOLEAN DEFAULT FALSE;

ALTER TABLE impulses 
ADD COLUMN selected_participant_id UUID REFERENCES profiles(id);
```

### Таблица `event_requests` (Заявки на участие)

```sql
CREATE TABLE event_requests (
  id SERIAL PRIMARY KEY,
  event_id INTEGER NOT NULL REFERENCES impulses(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  user_name TEXT,
  user_avatar TEXT,
  user_gender TEXT CHECK (user_gender IN ('male', 'female', 'prefer_not_to_say')),
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'accepted', 'rejected')),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(event_id, user_id, status) WHERE status = 'pending'
);
```

## 🔧 Технические детали

### Активация режима

```typescript
// Обработчик активации через жест
const handleCircleGestureComplete = (center: { x: number; y: number }) => {
  setIsFlipping(true);
  setTimeout(() => {
    activateDuoMode();
    setIsFlipping(false);
    // Обновляем карту после разворота
    setMapRefreshTrigger(prev => prev + 1);
  }, 600);
};
```

### Создание Duo-события

```typescript
// В режиме Duo события по умолчанию становятся эксклюзивными
const isDuoEvent = isDuoMode;

await supabase.from('impulses').insert({
  creator_id: userId, // UUID
  content: messageContent,
  category: categoryId,
  is_duo_event: isDuoEvent, // true в режиме Duo
  location_lat: location.lat,
  location_lng: location.lng,
});
```

### Отправка запроса на участие

```typescript
import { sendDuoEventRequest } from './lib/duoEvents';

const result = await sendDuoEventRequest(eventId, userId); // userId - UUID
```

### Загрузка запросов (для создателя)

```typescript
import { loadDuoEventRequests } from './lib/duoEvents';

const requests = await loadDuoEventRequests(
  eventId,
  creatorId, // UUID
  genderFilter // 'male' | 'female' | 'all'
);
```

### Принятие/Отклонение запроса

```typescript
import { acceptDuoEventRequest, rejectDuoEventRequest } from './lib/duoEvents';

// Принять запрос (автоматически отклоняет остальные)
await acceptDuoEventRequest(requestId, eventId, creatorId); // creatorId - UUID

// Отклонить запрос
await rejectDuoEventRequest(requestId, eventId, creatorId); // creatorId - UUID
```

## 🗺️ Работа карты после разворота

**Проблема:** После разворота экрана (`transform: rotateY(180deg)`) карта может некорректно отображаться.

**Решение:**
1. Карта оборачивается в контейнер с классом `map-container`
2. CSS исключает карту из разворота: `transform: none !important`
3. После завершения анимации вызывается `invalidateSize()` для обновления размеров
4. Карта автоматически центрируется на пользователе после разворота

**CSS стили:**
```css
[style*="rotateY(180deg)"] .map-container,
[style*="rotateY(180deg)"] .leaflet-container {
  transform: none !important; /* Карта не должна вращаться */
  backface-visibility: visible;
}
```

**Обновление карты:**
```typescript
// После завершения анимации разворота
setTimeout(() => {
  setMapRefreshTrigger(prev => prev + 1); // Обновляет карту
}, 300);
```

## 📱 Использование

### Для создателя события:

1. Активируйте режим Duo (круговой жест + нажатие в центр)
2. Создайте событие (категория → описание → место и время)
3. Событие автоматически становится Duo-событием
4. Откройте событие на карте
5. Просмотрите список заявок на участие
6. При необходимости отфильтруйте заявки по полу
7. Выберите одного участника для встречи
8. Остальные заявки автоматически отклоняются

### Для участника:

1. Найдите Duo-событие на карте
2. Откройте детальное окно события
3. Нажмите кнопку "Отправить запрос на участие"
4. Дождитесь ответа от создателя события
5. Если ваш запрос принят, вы получите уведомление

## 🎨 UI компоненты

### DuoEventRequestsManager
Компонент для управления заявками на участие (для создателя события):
- Отображение списка заявок
- Фильтрация по полу
- Принятие/отклонение заявок
- Отображение выбранного участника

### DuoEventRequestButton
Компонент для отправки запроса на участие (для участника):
- Кнопка отправки запроса
- Отображение статуса отправки
- Haptic feedback при отправке

## ✅ Готово!

Режим **Linger Duo** полностью реализован:
- ✅ Активация через жест (круговой жест + нажатие в центр)
- ✅ Анимация разворота экрана (180 градусов)
- ✅ Темный эксклюзивный фон в режиме Duo
- ✅ Автоматическая установка `is_duo_event = true` при создании события в режиме Duo
- ✅ Система запросов на участие
- ✅ Фильтрация заявок по полу
- ✅ Выбор одного участника из списка заявок
- ✅ Приватность заявок (видны только создателю)
- ✅ Поле "Пол" в профиле
- ✅ Корректная работа карты после разворота
- ✅ Центрирование карты на пользователе после разворота

**Приятного использования Linger Duo!** 💜
