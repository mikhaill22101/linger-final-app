# Исправление ошибок регистрации и синхронизация полей

## ✅ Исправленные проблемы

### 1. Поле `gender` в таблице profiles

**Проблема:** Поле `gender` использовалось в коде, но отсутствовало в SQL миграции для таблицы `profiles`.

**Решение:**
- Создана миграция `004_add_gender_field.sql` для добавления поля `gender`
- Поле `gender` необязательное (NULL) для поддержки существующих пользователей
- Обновлен триггер `handle_new_user` для включения поля `gender`

**SQL миграция:**
```sql
-- supabase/migrations/004_add_gender_field.sql
ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS gender TEXT CHECK (gender IN ('male', 'female', 'prefer_not_to_say'));

ALTER TABLE profiles 
ALTER COLUMN gender DROP NOT NULL;
```

### 2. Детальный вывод ошибок

**Проблема:** Пользователи видели общие сообщения об ошибках типа "Ошибка регистрации" без деталей.

**Решение:**
- Добавлен детальный вывод ошибок через `console.error` с полной информацией
- Сообщения об ошибках теперь содержат конкретные причины:
  - `23505`: User profile already exists (профиль уже существует)
  - `23503`: Foreign key constraint violation
  - `23502`: Required field is missing
  - `42P01`: Table does not exist (нужно применить миграции)
- Добавлен вывод через `alert` или `Telegram.WebApp.showAlert` для пользователя

**Примеры сообщений об ошибках:**
- "Email already registered. Please sign in instead."
- "Password does not meet requirements (minimum 6 characters)"
- "Invalid email address"
- "Required field is missing: gender"
- "Table profiles does not exist. Please run database migrations."

### 3. Конфликт формы регистрации с режимом Duo

**Проблема:** Форма регистрации могла переворачиваться вместе с интерфейсом в режиме Duo (180 градусов).

**Решение:**
- Добавлены CSS стили для исключения формы регистрации из разворота
- Форма регистрации всегда отображается в обычном, читаемом виде
- Используется `transform: none !important` для формы регистрации

**CSS стили:**
```css
/* Форма регистрации всегда отображается в обычном виде */
.auth-screen-container,
.auth-screen-container > *,
[style*="rotateY(180deg)"] .auth-screen-container,
[style*="rotateY(180deg)"] .auth-screen-container > * {
  transform: none !important;
  backface-visibility: visible;
}
```

### 4. Автоматический переход на главную страницу

**Проблема:** После успешной регистрации нужно было убедиться, что пользователь автоматически переходит на главную страницу.

**Решение:**
- Функция `handleAuthSuccess` автоматически устанавливает `isAuthenticatedUser = true`
- После успешной авторизации показывается главная страница вместо формы регистрации
- Карта автоматически инициализируется после получения геолокации
- Добавлен детальный лог процесса авторизации для отладки

**Процесс:**
1. Пользователь регистрируется → `signUpWithEmail`
2. Создается пользователь в Supabase Auth
3. Создается/обновляется профиль в таблице `profiles`
4. Вызывается `handleAuthSuccess` → `setIsAuthenticatedUser(true)`
5. Показывается главная страница с картой
6. Получается геолокация пользователя
7. Карта центрируется на пользователе
8. Загружаются данные приложения (feed, unread messages)

### 5. Обработка конфликтов триггера и ручного создания профиля

**Проблема:** Триггер `handle_new_user` автоматически создает профиль при регистрации, что могло конфликтовать с ручным созданием профиля в `signUpWithEmail`.

**Решение:**
- Используется `upsert` вместо `insert` для обновления существующего профиля или создания нового
- Ошибка `23505` (Unique constraint violation) обрабатывается как нормальная ситуация
- Если профиль уже существует (создан триггером), процесс продолжается без ошибки

**Код:**
```typescript
const { error: profileError } = await supabase
  .from('profiles')
  .upsert({
    id: data.user.id,
    email: email,
    full_name: fullName || null,
    gender: null, // Необязательное поле
    created_at: new Date().toISOString(),
    updated_at: new Date().toISOString(),
  }, {
    onConflict: 'id',
  });

// Ошибка 23505 (профиль уже существует) не критична
if (profileError && profileError.code !== '23505') {
  return { success: false, error: errorMessage };
}
```

## 📋 SQL миграции для применения

### 1. Добавить поле gender (если еще не добавлено)

```sql
-- supabase/migrations/004_add_gender_field.sql
ALTER TABLE profiles 
ADD COLUMN IF NOT EXISTS gender TEXT CHECK (gender IN ('male', 'female', 'prefer_not_to_say'));

ALTER TABLE profiles 
ALTER COLUMN gender DROP NOT NULL;

CREATE INDEX IF NOT EXISTS profiles_gender_idx ON profiles(gender) WHERE gender IS NOT NULL;
```

### 2. Обновить триггер handle_new_user

```sql
-- Обновление функции handle_new_user для включения поля gender
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email, phone, gender, created_at, updated_at)
  VALUES (
    NEW.id,
    NEW.email,
    NEW.phone,
    NULL, -- Поле gender необязательное
    NOW(),
    NOW()
  )
  ON CONFLICT (id) DO UPDATE
  SET 
    email = COALESCE(EXCLUDED.email, profiles.email),
    phone = COALESCE(EXCLUDED.phone, profiles.phone),
    gender = COALESCE(profiles.gender, EXCLUDED.gender),
    updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

## ✅ Результат

### Исправлено:

1. ✅ **Поле gender добавлено в таблицу profiles**
   - Поле необязательное (NULL)
   - Поддержка существующих пользователей
   - Индекс для фильтрации

2. ✅ **Детальный вывод ошибок**
   - Конкретные причины ошибок
   - Логирование в консоль
   - Показ пользователю через alert

3. ✅ **Форма регистрации не конфликтует с режимом Duo**
   - Всегда отображается в обычном виде
   - CSS исключает форму из разворота

4. ✅ **Автоматический переход на главную страницу**
   - После успешной регистрации
   - Карта инициализируется корректно
   - Геолокация получается автоматически

5. ✅ **Обработка конфликтов триггера**
   - Используется upsert для обновления/создания
   - Ошибка уникальности обрабатывается корректно

## 🔧 Тестирование

### Тест 1: Регистрация нового пользователя

1. Откройте форму регистрации
2. Введите Email, Пароль, Имя
3. Нажмите "Зарегистрироваться"
4. ✅ Пользователь должен автоматически перейти на главную страницу
5. ✅ Карта должна инициализироваться без ошибок
6. ✅ Геолокация должна быть получена автоматически

### Тест 2: Ошибка регистрации

1. Попробуйте зарегистрироваться с уже существующим email
2. ✅ Должно появиться сообщение: "Email already registered. Please sign in instead."
3. ✅ В консоли должен быть детальный лог ошибки

### Тест 3: Форма регистрации в режиме Duo

1. Активируйте режим Duo (круговой жест)
2. Выйдите из аккаунта (или откройте форму регистрации)
3. ✅ Форма регистрации должна отображаться в обычном виде (не перевернута)

### Тест 4: Поле gender при регистрации

1. Зарегистрируйте нового пользователя
2. Проверьте в Supabase Dashboard, что в таблице `profiles`:
   - Создан профиль с `id` = UUID из Supabase Auth
   - Поле `gender` = NULL (необязательное)
   - Поле `email` заполнено
   - Поле `full_name` заполнено (если указано)

## 📝 Примечания

- Поле `gender` можно указать позже в профиле пользователя
- Триггер `handle_new_user` автоматически создает профиль при регистрации в Supabase Auth
- Ручное создание профиля в `signUpWithEmail` использует `upsert` для избежания конфликтов
- Все ошибки логируются в консоль для отладки

## ✨ Готово!

Все ошибки регистрации исправлены:
- ✅ Поле gender добавлено и необязательное
- ✅ Детальный вывод ошибок
- ✅ Форма регистрации не конфликтует с режимом Duo
- ✅ Автоматический переход на главную страницу после регистрации
- ✅ Карта инициализируется без ошибок

**Примените SQL миграции в Supabase Dashboard для завершения!**
